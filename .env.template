# AppletScriptorium Environment Configuration
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control

# SMTP Configuration (Required for Mail rule automation)
# Gmail example (requires app password):
# 1. Visit https://myaccount.google.com/apppasswords
# 2. Create an app password for "Mail"
# 3. Use that password below (not your regular Gmail password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password-here
SMTP_FROM_EMAIL=your-email@gmail.com

# Optional: Jina Reader API key for article fetching fallback
# Used when url-to-md fails or is unavailable
#
# To get your API key:
# 1. Use https://www.browserling.com/ to access https://jina.ai/reader/
# 2. Click on "API KEY & BILLING" button
# 3. Sign up and copy your API key
# JINA_API_KEY=your-jina-api-key-here

# Optional: Custom HTTP headers for specific domains (JSON format)
# ALERT_HTTP_HEADERS_JSON='{"example.com": {"Cookie": "session=abc"}}'

# Email digest recipient (TO: address)
# This is typically your email address where you want to receive digests
ALERT_DIGEST_EMAIL=your-email@example.com

# Optional: Override sender email (defaults to SMTP_FROM_EMAIL)
# ALERT_EMAIL_SENDER=sender@example.com

# Optional: LLM model override (for Ollama fallback)
# ALERT_MODEL=qwen3:latest

# LM Studio API Configuration (Primary Backend)
# If set, LM Studio will be used as primary backend with optional Ollama fallback
LMSTUDIO_BASE_URL=http://192.168.1.11:1234
LMSTUDIO_MODEL=llama-chat-summary-3.2-3b
LMSTUDIO_TIMEOUT=30.0

# Ollama Configuration (Fallback Backend)
# WARNING: Ollama may significantly slow down your computer while processing articles
# Only configure if you want Ollama as fallback when LM Studio fails
# Leave commented to disable Ollama fallback entirely
# OLLAMA_ENABLED=true
# OLLAMA_MODEL=qwen3:latest
# OLLAMA_TIMEOUT=120.0

# Optional: Limit number of articles processed
# ALERT_MAX_ARTICLES=10
