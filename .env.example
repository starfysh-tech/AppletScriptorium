# AppletScriptorium Environment Configuration
# Copy this file to .env and fill in your values

# Jina Reader API Configuration (fallback for bot-protected sites)
# Get your API key at: https://jina.ai/reader/
JINA_API_KEY=your-jina-api-key-here

# Ollama Configuration
# Model to use for summarization (default: qwen3:latest)
ALERT_MODEL=qwen3:latest

# Email Digest Recipients
# Comma-separated list of email addresses to receive digest
ALERT_DIGEST_EMAIL=recipient1@example.com,recipient2@example.com

# Email Sender Address
# Optional sender address for digest emails (defaults to first recipient if not set)
ALERT_EMAIL_SENDER=sender@example.com

# HTTP Request Headers (JSON format)
# Custom headers for specific domains (e.g., authentication cookies)
# Example: ALERT_HTTP_HEADERS_JSON='{"example.com": {"Cookie": "session=abc123"}}'
ALERT_HTTP_HEADERS_JSON={}

# Gmail SMTP Configuration (for automated email sending)
# Required for sending emails via Gmail SMTP
# Generate app password at: https://myaccount.google.com/apppasswords
GMAIL_SMTP_USER=your-email@gmail.com
GMAIL_APP_PASSWORD=your-16-char-app-password
GMAIL_SMTP_HOST=smtp.gmail.com
GMAIL_SMTP_PORT=587
