# CommitCraft - Shell Aliases
#
# Add these aliases to your shell configuration:
#   - Bash: ~/.bashrc
#   - Zsh: ~/.zshrc
#   - Fish: ~/.config/fish/config.fish
#
# After adding, reload your shell:
#   source ~/.bashrc  # or ~/.zshrc

# =============================================================================
# INSTALLATION & SETUP
# =============================================================================

# Run global setup (installs ~/.claude/)
# NOTE: Replace path with your AppletScriptorium location
alias claude-setup='~/Code/AppletScriptorium/CommitCraft/commitcraft-install.sh'

# =============================================================================
# ANALYSIS & DEBUGGING
# =============================================================================

# Run pre-commit analysis in current repo
alias git-analyze='~/.claude/scripts/commitcraft-analyze.sh'

# Run pre-commit analysis and save to file for review
alias git-analyze-save='~/.claude/scripts/commitcraft-analyze.sh /tmp/git-analysis.txt && cat /tmp/git-analysis.txt'

# =============================================================================
# QUICK STATUS CHECKS
# =============================================================================

# Show sync status with remote
alias git-sync='git fetch origin --quiet && git status -sb'

# Show recent commits (last 10)
alias git-recent='git log --oneline -10'

# Show all untracked files
alias git-untracked='git ls-files --others --exclude-standard'

# =============================================================================
# SECURITY HELPERS
# =============================================================================

# Check for potential secrets in staged changes
alias git-secrets='git diff --cached | grep -iE "password|secret|api_key|token|credential|private_key" || echo "✓ No obvious secrets detected"'

# Check for TODO/FIXME markers in staged changes
alias git-todos='git diff --cached | grep -iE "TODO|FIXME|XXX|HACK" || echo "✓ No TODO markers in staged changes"'

# =============================================================================
# FILE SIZE CHECKS
# =============================================================================

# Show large file changes (>1000 lines) in staged changes
alias git-large='git diff --cached --stat | grep -E "\\|\\s+[0-9]{4,}\\s" || echo "✓ No unusually large files"'

# =============================================================================
# CUSTOMIZATION EXAMPLES
# =============================================================================

# Example: Add project-specific checks
# alias project-checks='pytest --collect-only && npm run lint'

# Example: Combined workflow
# alias git-ready='git-analyze && git-secrets && git-large && git status'
